/**
 * Lista de edades de usuarios
 * @type {Array}
 */

const UserAge = [18,20,30,70]
//colocar array es muy genérico, para hacerlo más descriptivo se hace de esta forma
/**
 * Lista de puntajes
 * @type {Array<Number>}
 */
const score =[10,3,24,100]

//para hacer poder hacer que el arrego contenga vrios tipos de datos se hace de la siguiente forma
/**
 * Lista de numeros y cadenas
 * @type {Array<Number | string >}
 */
const numbersAndString = [10, "2", 123, "234"]
